# Bot API token — never commit a real value.
# Create at https://lichess.org/account/oauth/token/create with bot scopes,
# then upgrade the account (irreversible):
#   curl -d '' https://lichess.org/api/bot/account/upgrade \
#     -H "Authorization: Bearer $LICHESS_TOKEN"
LICHESS_TOKEN=""
